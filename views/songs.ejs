<% include partials/header %>
<table>
  <% for(var i=0; i<songs.length; i++) {%>
  <tr>
      <td><%= songs[i].title %></td>
      <td><%= songs[i].album %></td>
      <td><%= songs[i].artist %></td>
      <td><%= songs[i].year %></td>
      <td><%= songs[i].bpm %></td>
  </tr>
  <% } %>
</table>
<% include partials/footer %>
